# עזר תורה – תושיה תפרח · איפיון מחדש, עיצוב חדש וסקירת אבטחה

מסמך זה מלווה את העיצוב מחדש של המערכת (index.html). הוא כולל: מיפוי מלא של המערכת הקיימת, מפרט העיצוב החדש (מצב יום/בהיר), רשימת שיפורים למשתמש ולמנהל, וסקירת אבטחת מידע עם רשימת פעולות נדרשת.

> המערכת היא אפליקציית קובץ-יחיד (SPA) בעברית RTL לניהול תורמים, תרומות, מגביות, מתרימים, קבוצות ומסלולי גיוס. הנתונים נשמרים ב-Supabase (ענן) עם גיבוי מקומי ב-localStorage.

---

## 1. מבנה המערכת (מיפוי)

### עמודים ראשיים (`showPage`)
| עמוד | תיאור |
|---|---|
| **ראשי (דשבורד)** | ברכה + תאריך עברי, 4 כרטיסי סיכום, כרטיס מגבית פעילה עם ספירה לאחור, ווידג'ט הכנסות/הוצאות, התראות חכמות, 2 גרפים (Chart.js), 8 תרומות אחרונות |
| **תורמים** | טבלה עם חיפוש חופשי, סינון מרובה (שיוך/מועד תרמה/עיר), טווח סכומים, בחירת עמודות (16), עימוד 200/עמוד, ייצוא Excel/PDF, מדבקות, גיאוקוד, עמוד עצמי |
| **תרומות** | טבלת כל התרומות, סינונים (תורם/אופן/מגבית/מתרים), טווח תאריכים, ייצוא והדפסה |
| **תרומות עתידיות** | תכנון תרומות עתידיות + המרה לתרומה בפועל |
| **מגביות** | יעד/נתרם/נותר/אחוז, פס התקדמות, לוחות מובילים (מתרימים, קבוצות, ועדים) |
| **מתרימים** | ניהול מתרימים, יעדים לפי מגבית, ייבוא, סטטיסטיקות |
| **קבוצות** | קבוצות מתרימים, קוד אוטומטי, חברים ותורמים |
| **מסלולים** | מפת Leaflet, יצירת מסלולי גיוס לפי שכונה, גרירה לשינוי סדר |
| **הוצאות** | מעקב הוצאות לפי מחלקה/סטטוס/חודש, סיכומים |
| **הודעות** | מסרים פנימיים בין משתמשים |
| **ניהול** | נוכחות משתמשים, סנכרון ענן, 14 כרטיסי ניהול (משתמשים, רשימות, ייבוא/ייצוא, מגביות, יומן, נדרים פלוס, EmailJS, ימות המשיח, כפילויות, אישור עריכות) |

### מודל הנתונים (`DB`)
`donors` (תורמים) · `donations` (תרומות, מקוננות בכל תורם) · `futureDonations` (עתידיות) · `relations` (התאמות) · `users` (משתמשים) · `campaigns` (מגביות) · `fundraisers` (מתרימים) · `groups` (קבוצות) · `fundraisingDates` (תאריכי גיוס) · `routes` (מסלולים) · `expenses` (הוצאות) · `messages` (הודעות) · `pendingEdits` (עריכות ממתינות) · `auditLog` (יומן פעולות) · `settings` (רשימות קשיחות) + מונים (`nextCode`, `nextFRCode`, `nextGrpSeq`).

### הרשאות (`can()`)
| יכולת | מנהל ראשי | מנהל | עורך | מבקר |
|---|:-:|:-:|:-:|:-:|
| צפייה | ✅ | ✅ | ✅ | ✅ |
| עריכה | ✅ | ✅ | ✅ | ❌ |
| ייצוא/הדפסה | ✅ | ✅ | ✅ | ❌ |
| ייבוא | ✅ | ✅ | ❌ | ❌ |
| פאנל ניהול | ✅ | ✅ | ❌ | ❌ |
| ניהול משתמשים | ✅ | ❌ | ❌ | ❌ |

### דוחות להדפסה
דוח תורמים מסונן · דף גיוס (קבוצה/עיר/שכונה) · דף גיוס מפורט לקבוצה (עם מפה) · דוח קבוצה · סקירת קבוצות · סיכום מגבית · מדבקות (Tanex) · חבילות מתרימים · הדפסת תרומות · הדפסת מסלולים · עמוד תרומה ציבורי.

---

## 2. העיצוב החדש — "אור" (Design System v30)

**החלטת עיצוב:** ערכת נושא **בהירה (מצב יום)** אחידה על כל המערכת — כולל הדשבורד, הטבלאות, המודאלים, מסך הכניסה, סרגל הצד, ודוחות ההדפסה.

### פלטת צבעים
- **ראשי — כחול ספיר** `#2360d8` (עם גוונים בהיר/כהה/רקע). נקי, מקצועי, נגיש.
- **הדגשה — ענבר חם** `#c07d10`. לפעולות משניות וכותרות.
- **רקע אפליקציה** `#f3f6fb` (כחלחל-בהיר), **משטחים** לבנים.
- **ניטרלים** — סקאלת "slate" קרירה (`stone-50…900`).
- **סמנטי** — הצלחה ירוק `#157347`, סכנה אדום `#c22430`, סגול, טורקיז.

### עקרונות
1. **קו עיצוב אחיד** — אותו רדיוס פינות, אותם צללים רכים כחלחלים, אותם כפתורים ותגים בכל עמוד.
2. **מודרני ודינמי** — כפתורי גרדיאנט עם הרמה עדינה ב-hover, אנימציות כניסת עמוד, פס התקדמות עם shimmer, מעברים חלקים.
3. **UX נוח** — סרגל צד לבן עם היררכיה ברורה והדגשת פריט פעיל, טופ-בר "זכוכית" בהיר, טבלאות עם hover, כרטיסים עם פס-צבע צדדי לזיהוי מהיר.
4. **נגישות** — טבעת פוקוס נראית (`:focus-visible`), ניגודיות תואמת WCAG, כיבוד `prefers-reduced-motion`.
5. **רספונסיביות** — נשמרו כל נקודות השבירה (768/480px), כרטיסי מובייל לתורמים, מודאלים כ-bottom-sheet במובייל.

### הטמעה
כל שמות המחלקות ומזהי-האלמנטים נשמרו — לכן **כל הלוגיקה (JS) ממשיכה לעבוד ללא שינוי**. השינוי הוא בשכבת העיצוב בלבד: בלוק ה-`<style>` הראשי הוחלף במלואו, וכל צבעי-החוץ הישנים (יין/נייבי כהה + זהב) מופו לפלטה החדשה גם בסגנונות המוטבעים וגם בתבניות ההדפסה.

---

## 3. תיקוני תקינות (באגים שנמצאו ותוקנו)

במהלך המיפוי אותרו באגים ששברו פיצ'רים — תוקנו כחלק מהעבודה:

1. **`getCurrentUser()` לא הוגדרה** — נקראה ב-`initSidebar()` וזרקה שגיאה שעצרה את המשך אתחול הכניסה (נוכחות, תגי הודעות, בדיקת גרסה). תוקן לשימוש ב-`currentUser`.
2. **רקורסיה אינסופית ב-`renderDash`** — שתי הגדרות של `renderDash` באותו scope גרמו ל-`_origRenderDash` ללכוד את עצמו. תוקן ע"י שינוי שם הפונקציה הבסיסית ל-`renderDashCore`.
3. **`checkResetToken()` לא הוגדרה** — כל כניסה עם `?reset=` בכתובת זרקה שגיאה. תוקן עם בדיקת קיום.
4. **`openDonorCard()` לא הוגדרה** — פריט "כרטיס הוספת תורם" בסרגל הצד היה שבור. חובר ל-`openNewDonorModal()`.

---

## 4. סקירת אבטחת מידע

> המערכת רצה כולה בצד הלקוח מול מפתח Supabase מסוג `anon`. חלק מהממצאים ניתנים לתיקון בצד הלקוח (בוצע), אך התיקונים המהותיים דורשים פעולה בצד השרת/התפעול.

### תוקן בקוד (צד לקוח)
- **XSS לא-מאומת בעמוד העצמי** — ערכי התורם הוזרקו ל-`value="..."` ללא escaping. הוסף `escHtml` לכל השדות.
- **הזרקת תרומות מזויפות דרך `postMessage`** — נוסף אימות `origin` (רק ממקורות נדרים פלוס) למאזין ההודעות.
- **Clickjacking** — נוסף frame-busting שמונע הטמעת המערכת ב-iframe עוין.

### דרוש טיפול דחוף (צד שרת / תפעול) — **לא ניתן לתקן מתוך הקובץ**
1. **החלפת כל המפתחות/סיסמאות מיידית** (בהנחה שנחשפו ב-Git): מפתח Supabase anon, מפתח ימות המשיח, מפתחות EmailJS, מפתח נדרים פלוס, וכל סיסמאות הצוות.
2. **הפעלת RLS ב-Supabase** על טבלת `app_state` — כרגע מפתח ה-anon מאפשר לכל אחד באינטרנט לקרוא/לכתוב/למחוק את כל בסיס הנתונים.
3. **מעבר ל-Supabase Auth** במקום השוואת סיסמאות בטקסט גלוי בצד הלקוח.
4. **אימות הרשאות בצד השרת** — כיום `can()` הוא קישוט ויזואלי בלבד וניתן לעקיפה דרך DevTools.
5. **הסרת סיסמאות ברירת מחדל** (`admin123` וכו') ו-`DEFAULT_USERS` מהקוד.
6. **מחיקת הקבצים הישנים** `donor-v18.html` ו-`donor-management-v9.html` — הם נושאים את אותם מפתחות חיים והם עודף מיותר.
7. **CSP + SRI** — הוספת מדיניות אבטחת תוכן והצמדת גרסאות + hash לסקריפטים חיצוניים (דורש מעבר מ-inline handlers).
8. **אחסון מקומי** — הפסקת שמירת סיסמה בטקסט גלוי ב-`savedSession` וניקוי `donorDB_v6` בהתנתקות.

---

## 5. שיפורים מומלצים (מעבר לעיצוב)

### למשתמש
- החזרת "החלף משתמש" (הוסר מאז v18) — כניסה מהירה בעמדות משותפות.
- כפתור "הדפסה/ייצוא" אחיד בכל טבלה (כרגע מפוזר).
- הודעות "toast" אחידות במקום `alert`/`confirm` בחלק מהזרימות.
- מצב "אין נתונים" מעוצב בכל טבלה ריקה.
- קיצורי מקלדת: Ctrl+K לחיפוש גלובלי כבר קיים — כדאי להוסיף רמז ויזואלי קבוע.

### למנהל
- החזרת התנתקות אוטומטית לפי חוסר פעילות (הושבתה ב-v26) — חשוב בעמדות משותפות.
- לוח בקרה למנהל: מגמות תרומות, יעדי מתרימים מול ביצוע, תורמים "נוטשים".
- ניקוי קוד מת (פונקציות מוצללות: `openImportModal`/`openExportWizard`/`generateReport` הישנות) לצמצום גודל וסיכון.
- הצמדת גרסאות CDN (כרגע `lucide@latest` משתנה ללא בקרה).

---

## 6. אימות

המערכת נבדקה בדפדפן (Chromium headless): כניסה, מעבר בין כל 11 העמודים, פתיחת מודאל תורם — ללא שגיאות JS מהשינויים. שגיאות הרשת בבדיקה נבעו מחסימת CDN בסביבת הבדיקה בלבד ואינן קיימות בסביבת ייצור.
