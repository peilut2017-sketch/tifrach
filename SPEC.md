# עזר תורה – תושיה תפרח · איפיון מערכת מאוחד (v30)

> מסמך האיפיון המלא של המערכת לאחר הבנייה מחדש. המערכת נבנתה במקור שלב-אחרי-שלב בלי סנכרון בין הצרכים; מסמך זה מאפיין את **כל** המודולים על בסיס אחד משותף — שכבת נתונים אחת, מנוע סנכרון אחד, מערכת עיצוב אחת, ומודל הרשאות אחד — ומפרט כל אופציה ופונקציה.

---

## 1. חזון ועקרונות הבסיס האחוד

המערכת משרתת ארגון צדקה שמנהל: תורמים ← תרומות ← מגביות שנתיות ← מתרימים (בחורים) ← קבוצות התרמה ← מסלולי גיוס פיזיים ← הוצאות. כל ישות נשענת על אותו בסיס:

1. **מקור אמת אחד** — אובייקט `DB` יחיד, נשמר כ-JSONB ב-Supabase (`app_state`/`main`) עם עותק מקומי ב-localStorage לעבודה לא-מקוונת.
2. **מנוע סנכרון אחד** — כל שמירה במערכת עוברת דרך `saveDB()` בלבד; הסנכרון, המיזוג והטיפול בהתנגשויות קורים במקום אחד (ראו §9). אף מודול לא כותב לענן ישירות.
3. **מערכת עיצוב אחת** — ערכת נושא בהירה "אור" (כחול ספיר + ענבר), אותם רכיבים (כפתורים, כרטיסים, טבלאות, מודאלים, טאבים) בכל עמוד ובכל דוח מודפס.
4. **מודל הרשאות אחד** — `can(perm)` נבדק בכל פעולה; ההזדהות דרך Supabase Auth; התפקיד נשמר בפרופיל (`DB.users`) לפי מייל.
5. **עקרון קישור אחיד** — כל ההצלבות בין ישויות הן לפי `id` פנימי (לא לפי שם): תרומה→מגבית (`campaignId`), תרומה→מתרים (`fundraiserId`) או →קבוצה (`groupId`), קבוצה→חברים (`memberIds`), קבוצה→תאריך גיוס (`fundraisingDateId`), קבוצה↔מסלולים (`routeIds`/`groupIds` דו-כיווני), תורם→התאמות (`relations[].donorId`).

---

## 2. ארכיטקטורה

```
┌───────────────────────── UI ─────────────────────────┐
│ 11 עמודים · ~35 מודאלים · 16 דוחות הדפסה · עיצוב v30 │
└──────────────────────────┬───────────────────────────┘
┌──────────────────────────▼───────────────────────────┐
│ לוגיקה עסקית — פונקציות render*/save*/open* למודול   │
└──────────────────────────┬───────────────────────────┘
┌──────────────────────────▼───────────────────────────┐
│ Store: אובייקט DB + saveDB() + loadDB()              │
│ Sync: מיזוג תלת-כיווני + CAS + poll חי (10 שנ')      │
└──────────────────────────┬───────────────────────────┘
┌──────────────────────────▼───────────────────────────┐
│ Supabase: Auth (הזדהות) · app_state (RLS: מחוברים    │
│ בלבד) · Edge Functions (עמוד עצמי, ימות)             │
└──────────────────────────────────────────────────────┘
```

**אינטגרציות חוץ:** EmailJS (מיילים), נדרים פלוס (אשראי, iframe + postMessage מאומת-origin), ימות המשיח (IVR דרך Edge Function), Nominatim/Leaflet (גיאוקוד ומפות), open.er-api (שערי מט"ח), SheetJS (אקסל), Chart.js (גרפים).

---

## 3. ישויות ונתונים

| ישות | מזהה | שדות עיקריים | הערות |
|---|---|---|---|
| **תורם** `donors[]` | `id` (D+ts), `code` (T1001+) | תואר, שם פרטי/משפחה, שם סגירה, שיוך, כתובת+כניסה, טלפון, סלולרי, מייל, ת"ז (עם ולידציית ספרת ביקורת), מועד תרמה, שנת נישואין, מחזור, סטטוס (פעיל/לא פעיל/נפטר/מיעוט), דירוג 1–5, הערות, lat/lng+עיר/שכונה מגיאוקוד, updatedAt/updatedBy | מכיל שלושה מערכים מקוננים ↓ |
| **תרומה** `donor.donations[]` | `id` (dn…) | סכום ₪, סכום+מטבע מקור (USD/EUR), תאריך, אופן (מזומן/אשראי/צ'ק/העברה), `campaignId`, שיוך ל-`fundraiserId` **או** `groupId`, הערות | `id` יציב — נדרש למיזוג בין משתמשים |
| **תרומה עתידית** `donor.futureDonations[]` | `id` (fd…) | תאריך צפוי, סכום צפוי, מגבית, הערות | ניתנת ל"אישור" → הופכת לתרומה |
| **התאמה** `donor.relations[]` | — | `donorId` (או `fr_<id>` למתרים), סוג קשר | קשר הדדי נוצר אוטומטית (הורה↔בן וכו') |
| **משתמש** `users[]` | מייל | displayName, role, firstLogin | **ללא סיסמה** — סיסמאות רק ב-Supabase Auth |
| **מגבית** `campaigns[]` | `id` (C+ts) | שם, שנה, יעד ₪, דדליין+שעה, פעילה (אחת בלבד), תיאור | |
| **מתרים** `fundraisers[]` | `id` (FR+ts), `code` (M0001+) | שם, ת"ז, וועד (א–ט), 3 טלפונים+מועדף, יעדים לפי מגבית `campaignTargets` | |
| **קבוצה** `groups[]` | `id` (G+ts), `code` | קוד = ספרת וועד + אינדקס תאריך גיוס + רץ; שם, וועד (רוב חברים), `fundraisingDateId`, `memberIds[]`, `donorIds[]`, `routeIds[]`, יעד, הערות | אכיפה: מתרים לא יכול להיות ב-2 קבוצות באותו תאריך גיוס |
| **תאריך גיוס** `fundraisingDates[]` | `id` (FD+ts) | תווית, תאריך, סדר | מחיקה נחסמת אם קבוצה משתמשת |
| **מסלול** `routes[]` | `id` (r_…) | קוד לפי עיר, שכונה, צבע, `donors[]` (סדר עצירות), `groupIds[]` | נוצר באשף לפי אלגוריתם שכן-קרוב |
| **הוצאה** `expenses[]` | `id` (E+ts) | מחלקה, תאריך, מטרה, סכום, אופן, משלם, מוטב+טלפון, סטטוס, פירוט | רשימות מחלקה/אופן/סטטוס ניתנות להרחבה |
| **הודעה** `messages[]` | `id` (MSG+ts) | מאת/אל (משתמש), נושא, גוף, נקרא, שידור לכולם | |
| **עריכה ממתינה** `pendingEdits[]` | `id` (PE+ts) | donorId, שדות שהוצעו, טוקן | מגיע מהעמוד העצמי, באישור מנהל |
| **יומן פעולות** `auditLog[]` | `id` (AL+ts) | סוג, תיאור, משתמש, זמן, נתוני שחזור | 500 אחרונות; undoData נשמר רק מקומית |
| **הגדרות** `settings` | — | תארים, שיוכים, שמות סגירה, מועדי תרמה, אופני תשלום, סוגי קשר | כל רשימה ניתנת לעריכה בפאנל ניהול |
| מונים | — | `nextCode`, `nextFRCode`, `nextGrpSeq{}` | במיזוג נלקח המקסימום — קוד לא חוזר |

---

## 4. הרשאות

| יכולת | מנהל ראשי | מנהל | עורך | מבקר |
|---|:-:|:-:|:-:|:-:|
| צפייה בכל העמודים | ✅ | ✅ | ✅ | ✅ |
| הוספה/עריכה (תורמים, תרומות, מתרימים, קבוצות) | ✅ | ✅ | ✅ | ❌ |
| ייצוא והדפסה | ✅ | ✅ | ✅ | ❌ |
| ייבוא מאקסל | ✅ | ✅ | ❌ | ❌ |
| פאנל ניהול, אשף ייצוא, הגדרות | ✅ | ✅ | ❌ | ❌ |
| ניהול משתמשים ותפקידים | ✅ | ❌ | ❌ | ❌ |

**זרימת כניסה:** Supabase Auth (מייל+סיסמה, אימות בשרת) → טעינת DB → איתור פרופיל לפי מייל → אם אין פרופילים כלל, המשתמש הראשון הופך אוטומטית למנהל ראשי → אם המייל לא מוכר, כניסה נדחית ("אינו מורשה"). שחזור סשן דרך `getSession` (ללא שמירת סיסמה). שינוי סיסמה — `auth.updateUser`.

---

## 5. איפיון מסך-מסך

### 5.1 ראשי (דשבורד)
- ברכה אישית + תאריך לועזי ועברי.
- פעולות מהירות: + הוסף תורם · + הוסף תרומה · 💳 תרומת אשראי (מוצג רק אם נדרים פלוס מוגדר).
- 4 כרטיסי מדד: תורמים רשומים (→ עמוד תורמים), סך תרומות, תרומות שנרשמו, עתידיות (→ עמוד עתידיות).
- כרטיס מגבית פעילה: יעד/נתרם/נותר/אחוז + פס התקדמות + ספירה לאחור לדדליין.
- ווידג'ט הכנסות מול הוצאות (+ יתרה).
- התראות חכמות: מגבית מתחת ליעד, עתידיות מתקרבות, תורמים "רדומים" (365+ יום), תורמים ללא כתובת — כל התראה לחיצה שמסננת את הרשימה המתאימה.
- 2 גרפים: תרומות לפי חודש (12 חודשים), מתרימים מובילים.
- 8 תרומות אחרונות.
- חיפוש גלובלי Ctrl+K — תורמים, מתרימים, קבוצות.

### 5.2 תורמים
- חיפוש חופשי בכל השדות + סינון מרובה (שיוך / מועד תרמה / עיר) + טווח סך-תרומות.
- בחירת עמודות (16 עמודות, נשמרת ב-localStorage) · מיון בכל כותרת · עימוד 200 לעמוד.
- בחירה מרובה עם סרגל פעולות (מחיקה מרוכזת).
- פעולות שורה: עריכה, וואטסאפ (נרמול 0→972), מייל, מחיקה.
- כלי עמוד: ייצוא Excel/PDF מהיר לפי הסינון, אשף ייצוא, מדבקות, מייל מרוכז, קישורי עמוד-עצמי, גיאוקוד קבוצתי (Nominatim עם השהיית 1.1 שנ'), הדפסה.
- **כרטיס תורם (מודאל)** — 6 טאבים:
  1. **תורם** — כל השדות, ולידציה חיה לת"ז, התרעת כפילות חיה (טלפון/מייל/ת"ז), השלמת כתובת עם תצוגת מפה.
  2. **תרומות** — טבלה + טופס הוספה מוטבע עם המרת מט"ח חיה ושיוך למתרים/קבוצה.
  3. **עתידיות** — תכנון + אישור-כתרומה.
  4. **התאמות** — קשרי משפחה לתורמים/מתרימים, הדדיות אוטומטית.
  5. **חוזק קשר** — ציון מחושב (תדירות, עדכניות, עקביות, מגמת סכומים) + ציר זמן.
  6. **היסטוריה** — כל פעולות היומן על התורם.
- תורם חדש שטרם נשמר: תרומות/עתידיות/התאמות שנוספו נאגרות ומוצמדות אוטומטית בשמירה.

### 5.3 תרומות
טבלה שטוחה של כל התרומות: סינון לפי תורם/אופן/מגבית/מתרים + טווח תאריכים, מיון, סיכום מסונן, ייצוא אקסל והדפסה. עריכה פותחת את כרטיס התורם בטאב תרומות.

### 5.4 עתידיות
כל התרומות המתוכננות: חיפוש, מיון (תאריך/שם/סכום), תצוגת תאריך עברי, מעבר לתורם, אישור/מחיקה.

### 5.5 מגביות
ניהול (בפאנל ניהול): יצירה/עריכה/מחיקה, הפעלת מגבית מכבה את הקודמת. תצוגה: יעד ענק צבעוני, פס התקדמות, יעד-תאריך + ספירה, 10 מתרימים מובילים, 10 קבוצות מובילות, פילוח לפי וועד, תאריכי גיוס (CRUD).

### 5.6 מתרימים
טבלה עם סטטיסטיקות (סה"כ גויס = ישיר + חלק יחסי בקבוצות), יעד מול ביצוע למגבית הפעילה, בחירה מרובה, ייבוא אקסל עם ממפה-עמודות, בדיקת כפילויות (ת"ז/טלפונים), הדפסת רשימה.

### 5.7 קבוצות
כרטיסי קבוצה עם סינון (חיפוש/וועד/תאריך גיוס), קוד אוטומטי חי, שיבוץ חברים ותורמים בצ'יפים, קישור למסלולים, יעד מול גיוס, דוח קבוצה ודף גיוס מודפסים.

### 5.8 מסלולים
מפת Leaflet מלאה: אשף יצירה (עיר, תורמים-למסלול, מרחק מקסימלי) → גיאוקוד → קיבוץ לפי שכונה (עם עורך כללי-שכונות) → סידור שכן-קרוב → פיצול. סרגל שכונות/מסלולים, פאנל עצירות עם גרירה לשינוי סדר, צבע לכל מסלול, הדפסת כל המסלולים.

### 5.9 הוצאות
טבלה עם סינון (חיפוש/מחלקה/סטטוס/חודש), רשימות בחירה מתרחבות ("+ ערך חדש" נשמר), סיכום לפי מחלקה וסיכום חודשי, ייצוא אקסל.

### 5.10 הודעות
דואר פנימי: שרשורים לפי משתמש, שידור לכולם, מונה לא-נקראו בכותרת העליונה ובסרגל.

### 5.11 ניהול
נוכחות חיה (מי מחובר, באיזה עמוד, כמה זמן) · מצב סנכרון ענן + בדיקת חיבור · משתמשים (מייל, שם, תפקיד; ללא סיסמאות) · עריכת רשימות קשיחות · ייבוא תורמים (ממפה-עמודות) · ייבוא נדרים פלוס (התאמה אוטומטית לפי טלפון/שם/ת"ז) · ייצוא מלא (3 גיליונות) · אשף ייצוא והדפסה · יומן פעולות עם שחזור (Undo) · הגדרות נדרים פלוס / EmailJS / ימות המשיח · איתור כפילויות + מיזוג תורמים · אישור עריכות מהעמוד העצמי.

---

## 6. אשף הייצוא וההדפסה (מרכז אחוד)

4 שלבים: (1) סוג פלט — אקסל / PDF תורמים / חבילות מתרימים / מדבקות; (2) סינון — שיוך, עיר, סכום מינימלי, מגבית, קבוצה, מועד תרמה, טווח תאריכים; (3) בחירת עמודות/תוכן; (4) תצוגה מקדימה → הפקה. בנוסף כפתורי "ייצוא מהיר" בכל טבלה (אקסל/PDF לפי הסינון הפעיל).

**דוחות הדפסה (16):** דוח תורמים מסונן (כולל 3 מגביות אחרונות בעמודות) · דף גיוס לפי קבוצה/עיר/שכונה · דף גיוס מפורט לקבוצה עם מפה ועמודת רישום-ידני · דוח קבוצה · סקירת קבוצות · סיכום מגבית · מדבקות (3 מחוללים — Tanex 3100/3101/3102/3106, בונה-שורות בגרירה, גופן וגודל) · חבילות מתרימים (עמוד לקבוצה + מפה) · תרומות מסוננות · מסלולים · רשימת מתרימים · הדפסת עמוד נוכחי. כולם בקו העיצוב האחיד (כחול ספיר + ענבר, Heebo).

---

## 7. עמוד עצמי לתורם

קישור ייחודי לכל תורם (`?selfService=<token>&donor=<id>`). מאז המעבר ל-RLS הטופס עובר דרך **Edge Function** (`self-service`): קריאת השדות ושליחת העדכון מאומתות בטוקן **בצד השרת**, מוגבלות ל-6 שדות בלבד. העדכון נכנס ל"עריכות ממתינות" ומחכה לאישור מנהל.

---

## 8. תשלומים ותקשורת

- **נדרים פלוס:** iframe תשלום עם פרטי תורם; קליטת התוצאה ב-postMessage מאומת-origin בלבד; התרומה נרשמת אוטומטית עם אסמכתא.
- **EmailJS:** מייל לתורם בודד / מרוכז עם שדות-מיזוג ({{שם}}, {{סכום}}…), עורך עשיר, תבניות.
- **ימות המשיח:** Webhook ב-Edge Function לרישום תרומות טלפוניות (אופציונלי).

---

## 9. עבודה מרובת-משתמשים — מנוע הסנכרון החדש

**הבעיה הישנה:** התראת "ניגוד שינויים" הוקפצה גם על עריכות של המשתמש עצמו (כי הלקוח השווה חותמת-זמן שהוא יצר מול חותמת שהשרת שכתב), והכפתור "טען גרסה חדשה" דרס את העריכות המקומיות. בנוסף, "דרוס בכל זאת" מחק בשקט עבודה של אחרים.

**הפתרון החדש — אין דיאלוגים, אין דריסות:**

1. **בסיס משותף (`_baseDB`)** — המערכת זוכרת בדיוק מה הגרסה האחרונה שידוע שנמצאת בשרת.
2. **מיזוג תלת-כיווני אוטומטי** — כשמגיעה גרסה חדשה מהשרת, כל רשומה (תורם, תרומה, קבוצה…) מושווה מול הבסיס:
   - שינוי רק בצד אחד → מתקבל אוטומטית.
   - תורם ששונה בשני הצדדים → מיזוג **ברמת שדה בודדת** (אחד עדכן טלפון והשני כתובת — שניהם נשמרים) והמערכים המקוננים (תרומות/עתידיות/התאמות) מתמזגים רשומה-רשומה.
   - אותו שדה ממש שונה בשני הצדדים → העדכני מנצח (לפי updatedAt), והמקרה נספר.
   - מחיקה מצד אחד + אי-שינוי מהצד השני → נמחק; מחיקה מול **עריכה** → העריכה שורדת (בטוח יותר).
   - מונים (`nextCode` וכו') → מקסימום, כך שקוד תורם לא יוקצה פעמיים לאחור.
3. **כתיבה מותנית (CAS)** — השמירה לשרת מתבצעת רק אם הגרסה בשרת היא זו שמיזגנו מולה (`update … eq(updated_at)`); אם מישהו הקדים — מושכים, ממזגים ומנסים שוב. **אי-אפשר לדרוס בטעות.**
4. **סנכרון חי** — כל 10 שניות המערכת בודקת אם יש גרסה חדשה וממזגת אוטומטית; מוצג toast "שינויים של משתמש אחר שולבו". אם מודאל פתוח (משתמש באמצע טופס) — רענון התצוגה נדחה לסגירת המודאל, כדי לא לדרוס הקלדה.
5. **רענון ידני** — כפתור "רענן" מושך-וממזג (לעולם לא דורס עריכות מקומיות).
6. **עבודה לא-מקוונת** — כל שמירה נכתבת מיד ל-localStorage; כשל סנכרון מסומן בנקודת הסטטוס וחוזר על עצמו אוטומטית כל 7 שניות עד הצלחה.
7. **מזהים יציבים** — לכל תרומה/עתידית מוקצה `id` ייחודי (כולל הסבה חד-פעמית לנתונים ישנים), כדי שמיזוג בין לקוחות יזהה רשומות זהות.

**מה עוד מקטין התנגשויות בפועל:** נוכחות חיה בפאנל הניהול מראה מי מחובר ובאיזה עמוד; העריכות נעשות במודאלים קצרים (שניות עד דקות) והמיזוג ברמת שדה הופך את רוב ההתנגשויות ללא-רלוונטיות.

---

## 10. בעיות שאותרו — מה תוקן ומה מומלץ

### תוקן בבנייה מחדש
| בעיה | מה נעשה |
|---|---|
| התראת התנגשות שגויה + אובדן עריכות | הוסרה לחלוטין; הוחלפה במיזוג אוטומטי + CAS (ראו §9) |
| `getCurrentUser` לא מוגדרת — שברה את אתחול הכניסה (נוכחות, תגים) | תוקן |
| רקורסיה אינסופית ב-`renderDash` (שתי הגדרות באותו scope) | תוקן (`renderDashCore`) |
| `checkResetToken` לא מוגדרת — `?reset=` הפיל את הדף | תוקן |
| "כרטיס הוספת תורם" בסרגל — פונקציה לא קיימת | חובר ל-`openNewDonorModal` |
| כפתור "רענן" נצבע בצבעי הערכה הכהה הישנה אחרי שמירה | תוקן |
| קריסת הדף כשה-CDN של Supabase לא נטען | `_sb` מוגן — המערכת עולה במצב לא-מקוון |
| סיסמאות בטקסט גלוי (ברירת מחדל, "זכור אותי", טבלת משתמשים) | הוסרו; הזדהות רק ב-Supabase Auth |
| מסד פתוח לכל האינטרנט (anon מלא) | RLS למחוברים בלבד + Edge Functions |
| XSS בעמוד העצמי; postMessage לא מאומת; clickjacking | תוקנו |
| קוד מת/מוצל: `openImportModal` ישן, אשף ייצוא ישן + המודאל שלו, `generateReport` | נמחקו (~150 שורות) |

### בוצע בסבב ההמשך
1. ✅ **נעילת עריכה רכה לכרטיס תורם** — באנר "✏️ X עורך כרטיס זה כעת" (דרך רשומות הנוכחות), לא חוסם — המיזוג מגבה.
2. ✅ **סנכרון מיידי** — ערוץ Realtime broadcast: כל שמירה מוצלחת משדרת לשאר הלקוחות למשוך-ולמזג מיד; ה-poll של 10 שניות נשאר כגיבוי. לא דורש הגדרות בפרויקט.
3. ✅ **התנתקות אוטומטית** — 30 דקות חוסר פעילות, אזהרת דקה, איפוס חסכוני (throttle 5 שנ').
4. ✅ **איפוס סיסמה מאובטח** — resetPasswordForEmail של Supabase + טיפול ב-PASSWORD_RECOVERY; מנגנון הקודים הישן (EmailJS + ‎_resetCodes במסד) הוסר.
5. ✅ **ניקוי UI מסיסמאות** — עמודת "סיסמה ראשונית" הוסרה מניהול משתמשים; "זכור אותי" הוסר (סשן Supabase מתמיד ממילא).
6. ✅ **הצמדת גרסאות CDN** — lucide 0.454.0, supabase-js 2.49.4, emailjs 4.4.1, xlsx שודרג ל-0.20.3 (CDN רשמי של SheetJS — 0.18.5 עם חולשה ידועה).
7. ✅ **סריקת XSS שיטתית** — escHtml על תוכן שהוזן ע"י משתמשים בכל נקודות ה-innerHTML המרכזיות (הודעות, עריכות ממתינות, טבלאות, דוחות מודפסים).
8. ✅ **מחיקת הקבצים הישנים** donor-v18.html, donor-management-v9.html.

### מומלץ להמשך (דורש החלטה/עבודה נוספת)
1. **נרמול הנתונים לטבלאות אמיתיות** (donors, donations…) — הצעד הארכיטקטוני הבא: יבטל לגמרי את בלוב-הענק, יאפשר הרשאות עדינות בשרת והיסטוריה מלאה. שינוי גדול — מומלץ רק כשהמערכת תתייצב.
2. **SRI (integrity hashes) לסקריפטים** — הגרסאות הוצמדו; חישוב ה-hash דורש גישה ל-CDN.
3. **כרטיס נדרים פלוס:** מפתח ה-API עובר ב-URL של ה-iframe (מגבלת הספק) — לוודא מול נדרים פלוס אם קיים מנגנון עדכני יותר.
4. **ניקוי היסטוריית Git** מהמפתחות הישנים (git filter-repo) — הקבצים נמחקו מה-HEAD אך קיימים בהיסטוריה; המפתחות ממילא הוחלפו במעבר לפרויקט החדש.

> פירוט מלא של כל מה שירד/הוחלף — במסמך **WHATS-CHANGED.md**.

---

## 11. מפת פונקציות (תמצית לפי מודול)

- **Store/Sync:** `loadDB` · `saveDB` · `_ensureRecordIds` · `_merge3DB`/`_merge3List`/`_mergeDonorDeep`/`_merge3Object` · `_pushWithCAS` · `_applyRemoteMerge` · `_syncToSupabase` · `_pollForCloudChanges` · `manualRefresh` · `_showSyncStatus` · `renderCurrentPage`
- **Auth:** `doLogin` · `_afterAuth` · `_setupSession` · `doLogout` · `saveNewPassword` · `can` · `setupNavForRole`
- **תורמים:** `renderAll` · `applyFilters` · `sortAndRender` · `openDonorModal`/`openNewDonorModal` · `fillForm` · `saveDonor`/`saveDonorAndNew` · `deleteDonorRow` · `switchDTab` · ולידציות (`validateIsraeliId`, `checkFieldDuplicate`) · גיאוקוד (`addressAutocomplete`, `rpGeocodeOne`, `runGeocodeBatch`)
- **תרומות:** `saveDonation` · `saveQuickDonation` · `renderDonationsPanel` · `editDonInPanel` · המרות מט"ח (`getExchangeRates`, `updateCurrencyConversion`) · עתידיות (`saveFutureDonation`, `confirmFutureDonation`, `renderFuturePage`)
- **מגביות:** `saveCampaign` · `renderCampaignPage` · `buildCampFRStats` · `startCampaignCountdown` · תאריכי גיוס (`saveFundraisingDate`…)
- **מתרימים/קבוצות:** `saveFR` · `renderFRList` · `frRaisedTotal` · `checkFRDuplicates` · `saveGroup` · `genGrpCode` · `renderGroupsPage`
- **מסלולים:** `rpGenerate` · `rpNearestNeighbor` · `rpSplitByGap` · `renderRoutesPage` · `rpDraw*` · גרירה (`rpDrag*`) · `rpPrint`
- **הוצאות:** `saveExpense` · `renderExpenses` · `renderExpSummary` · `exportExpensesExcel`
- **ייבוא/ייצוא:** `impLoadFile`/`impDoImport` (ממפה-עמודות) · `frImp*` · `importNedarimRows` · `openExportWizard` (v2) · `wizDo*` · `quickExcelExport`/`quickPdfExport` · `exportExcel`
- **הדפסה:** `printReport` · `runFrPrint` · `printFundraisingSheet` · `printGroupReport` · `printGroupsOverview` · `printCampaignSummary` · `printLabels`/`runPrintLabels`/`wizDoPdfLabels` · `printDonationsPanel` · `rpPrint`
- **הודעות/מיילים:** `sendMessage` · `renderMsgThreads` · `openMsgThread` · `sendBulkEmails` · `applyMergeFields`
- **ניהול:** `renderUsersTable`/`saveUsers` · `openSettingsModal` · `auditLog`/`undoAuditEntry` · `findDuplicateDonors`/`confirmMerge` · `renderPendingEdits`/`approvePendingEdit` · `renderPresenceList`
- **עמוד עצמי:** `checkSelfServiceMode` · `showSelfEditForm` · `submitSelfEdit` (דרך Edge Function)
